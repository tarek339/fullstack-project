# Development Dockerfile für NestJS
FROM node:18-alpine

# Arbeitsverzeichnis setzen
WORKDIR /app

# Package files kopieren
COPY package*.json ./

# Alle Dependencies installieren (including dev dependencies)
RUN npm ci

# Source code wird über Volume gemountet
# Port exponieren
EXPOSE 3000

# Standard-Command (wird in docker-compose überschrieben)
CMD ["npm", "run", "start:dev"]