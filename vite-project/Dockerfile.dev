# Development Dockerfile für Vite React
FROM node:18-alpine

# Arbeitsverzeichnis setzen
WORKDIR /app

# Package files kopieren
COPY package*.json ./

# Alle Dependencies installieren
RUN npm ci

# Source code wird über Volume gemountet
# Port exponieren (Vite default)
EXPOSE 5173

# Standard-Command (wird in docker-compose überschrieben)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]